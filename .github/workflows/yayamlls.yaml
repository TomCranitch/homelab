# TODO: re-enable yayamlls validation once pre-existing noise is cleaned up.
# Currently blocks PR #1735 on:
#   - SOPS schema load failures (non-JSON body from ops-schema)
#   - ${VAR} hostname pattern mismatches (resolved by Flux at deploy time)
#   - securityContext string-vs-int type errors in many HelmReleases
#   - authentic blueprint schema mismatches
#   - talos/cni/install.yaml parse error
#
# Intended workflow — reinstate after fixing above:
#
# ---
# name: Validate YAML
#
# on:
#   push:
#     paths:
#       - "**/*.yaml"
#       - "**/*.yml"
#   pull_request:
#     paths:
#       - "**/*.yaml"
#       - "**/*.yml"
#
# jobs:
#   validate:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v7
#
#       - uses: actions/setup-go@v6.5.0
#         with:
#           go-version: "1.26"
#
#       - name: Install yayamlls
#         run: go install github.com/home-operations/yayamlls/cmd/yayamlls@latest
#
#       - name: Get changed YAML files
#         run: |
#           git fetch origin "${{ github.base_ref }}"
#           files=$(git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- '*.yaml' '*.yml' || true)
#           echo "changed_files=$files" >> "$GITHUB_ENV"
#
#       - name: Validate changed manifests
#         run: |
#           if [ -z "$changed_files" ]; then
#             echo "No YAML files changed, skipping"
#             exit 0
#           fi
#           echo "Validating: $changed_files"
#           yayamlls validate $changed_files
