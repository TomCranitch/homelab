apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: digitalocean-issuer
  namespace: ingress
spec:
  acme:
    solvers:
      - dns01:
          digitalocean: "${S_DIGITAL_OCEAN_ACCESS_TOKEN}"
